import React from 'react';
import Todos from "../components/todosContainer/Todos";

const TodosPage = () => {
    return (
        <div>
            <Todos />
        </div>
    );
};

export default TodosPage;